package com.youtube.jwt.service;
/*
import com.youtube.jwt.entity.Email;

public interface EmailService {
    String sendSimpleMail(Email details);

    String sendMailWithAttachment(Email details);

}*/
/*
import org.springframework.mail.SimpleMailMessage;
import org.springframework.mail.javamail.JavaMailSender;
import org.springframework.stereotype.Service;


@Service
public class EmailService {
    JavaMailSender emailSender;
    public EmailService (JavaMailSender emailSender) {
        this.emailSender = emailSender;
        void sendMessage(String to, String subject, String text) {
            SimpleMailMessage message = new SimpleMailMessage();
            message.setFrom("greg.l. turnquist@gmail.com");
            nessage.setTo(to);
            message.setSubject (subject);
            nessage.setText (text);
            this.emailSender.send(nessage);}

    }*/

